# Direct runtime dependencies — these are what scripts import.
anthropic>=0.40
beautifulsoup4>=4.12
pdfplumber>=0.10
pillow>=10.0
requests>=2.32
yt-dlp>=2025.0
youtube-transcript-api>=0.6

# Optional: local-only Whisper fallback (CI sets USE_WHISPER_FALLBACK=false).
# Uncomment locally if you want to transcribe videos without YouTube captions.
# faster-whisper>=1.0

# Transitive pins below — kept for reproducibility on the runner.
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
charset-normalizer==3.4.7
distro==1.9.0
docstring_parser==0.18.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.16
jiter==0.15.0
pydantic==2.13.4
pydantic_core==2.46.4
sniffio==1.3.1
soupsieve==2.8.4
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
